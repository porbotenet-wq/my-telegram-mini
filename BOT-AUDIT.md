# Аудит Telegram Bot v4 — Полная карта экранов и ролей

## Общая архитектура
- Монолит: `telegram-bot/index.ts` — 1640 строк
- Shared: `_shared/botFSM.ts`, `botScreens.ts`, `botUtils.ts` — **НЕ ИСПОЛЬЗУЮТСЯ** ботом (бот дублирует всё внутри себя)
- Паттерн: один inline-message, редактируется через `tgEdit` (не спамит чат)
- Сессия: `bot_sessions` таблица, TTL 2ч (BUG-007: нужно 8ч)

## 10 ролей — Экраны

### 👔 Директор (prefix: `d`)
| Экран | Callback | Функция | Статус |
|-------|----------|---------|--------|
| Главное меню | `d:menu` | screenDirectorMenu | ✅ Работает |
| Портфель проектов | `d:portfolio` | screenPortfolio | ✅ Работает |
| KPI по отделам | `d:kpi` | screenKPI | ⚠️ Заглушка (данные из alerts/deficit/plan_fact) |
| Критические отклонения | `d:critical` | screenCritical | ✅ Работает |
| Финансы | `d:finance` | screenFinance | ⚠️ Заглушка ("доступно в приложении") |
| Дашборд | `d:dash` | screenDashboard (shared) | ✅ Работает |
| Алерты | `d:alerts` | screenAlerts (shared) | ✅ Работает |
| Снабжение | `d:supply` | screenSupply (shared) | ✅ Работает |
| Фасады | `d:facades` | screenFacades (shared) | ✅ Работает |
| Детали фасада | `d:fcd:<id>` | screenFacadeDetail (shared) | ✅ Работает |
| Согласования | `d:approvals` | screenApprovals (shared) | ✅ Работает |
| Журналы | `d:logs` | screenDailyLogs (shared) | ✅ Работает |
| Создать алерт | `d:alert_new` | screenAlertNew (shared) | ✅ Работает |
| Настройки | `c:settings` | screenSettings (shared) | ✅ Работает |

**Отсутствует:**
- Входящие (Inbox) — директор не видит входящие документы
- Отправка документов — директор не может отправлять

---

### 📋 Руководитель проекта / PM (prefix: `pm`)
| Экран | Callback | Функция | Статус |
|-------|----------|---------|--------|
| Главное меню | `pm:menu` | screenPMMenu | ✅ Работает (с динамическими счётчиками) |
| Входящие | `pm:inbox` | screenInbox (shared) | ✅ Работает |
| Отправить → категории | `pm:send` | screenPMSend | ✅ Работает |
| → Запуск проекта | `pm:s:launch` | screenPMSendLaunch | ✅ Работает |
| → Проектные работы | `pm:s:design` | screenPMSendDesign | ✅ Работает |
| → Снабжение | `pm:s:supply` | screenSupply (shared) | ⚠️ Показывает дефицит, не отправку |
| → Производство | `pm:s:prod` | screenDashboard (shared) | ⚠️ Показывает дашборд, не отправку |
| Быстрые действия | `pm:quick` | screenPMQuick | ✅ Работает |
| Обзор проекта | `pm:dash` | screenDashboard (shared) | ✅ Работает |
| Алерты | `pm:alerts` | screenAlerts (shared) | ✅ Работает |
| Задачи | `pm:tasks` | screenTasks (shared) | ✅ Работает |
| Согласования | `pm:approvals` | screenApprovals (shared) | ✅ Работает |

**Проблемы:**
- `pm:s:supply` и `pm:s:prod` ведут не туда (показывают shared-экраны вместо подкатегорий отправки)
- Нет экрана `pm:logs` в callback dispatcher (хотя функция есть)

---

### 📐 ОПР (prefix: `opr`)
| Экран | Callback | Функция | Статус |
|-------|----------|---------|--------|
| Главное меню | `opr:menu` | screenOPRMenu | ✅ Работает |
| Входящие | `opr:inbox` | screenInbox | ✅ Работает |
| Отправить | `opr:send` | screenOPRSend | ✅ Работает |
| Мой прогресс | `opr:progress` | screenProgress (shared) | ✅ Работает |

**Отсутствует:**
- Алерты, Фасады, Дашборд — нет кнопок в меню

---

### 📏 КМ (prefix: `km`)
| Экран | Callback | Функция | Статус |
|-------|----------|---------|--------|
| Главное меню | `km:menu` | screenKMMenu | ✅ Работает |
| Входящие | `km:inbox` | screenInbox | ✅ Работает |
| Отправить | `km:send` | screenKMSend | ✅ Работает |
| Мой прогресс | `km:progress` | screenProgress (shared) | ✅ Работает |

**Отсутствует:** то же что ОПР

---

### ✏️ КМД (prefix: `kmd`)
| Экран | Callback | Функция | Статус |
|-------|----------|---------|--------|
| Главное меню | `kmd:menu` | screenKMDMenu | ✅ Работает |
| Входящие | `kmd:inbox` | screenInbox | ✅ Работает |
| Отправить | `kmd:send` | screenKMDSend | ✅ Работает |
| Мой прогресс | `kmd:progress` | screenProgress (shared) | ✅ Работает |

**Отсутствует:** то же что ОПР

---

### 📦 Снабжение (prefix: `sup`)
| Экран | Callback | Функция | Статус |
|-------|----------|---------|--------|
| Главное меню | `sup:menu` | screenSupplyMenu | ✅ Работает (с дефицитом) |
| Входящие | `sup:inbox` | screenInbox | ✅ Работает |
| Отправить | `sup:send` | screenSupplySend | ✅ Работает |
| Статус закупок | `sup:status` | screenSupplyStatus | ⚠️ Заглушка (→ screenSupply) |
| Дефицит | `sup:deficit` | screenSupplyDeficit | ⚠️ Заглушка (→ screenSupply) |

**Проблемы:**
- `sup:status` и `sup:deficit` оба ведут на один и тот же shared screenSupply
- Нет отдельного экрана статуса закупок

---

### 🏭 Производство (prefix: `prod`)
| Экран | Callback | Функция | Статус |
|-------|----------|---------|--------|
| Главное меню | `prod:menu` | screenProductionMenu | ✅ Работает |
| Входящие | `prod:inbox` | screenInbox | ✅ Работает |
| Отправить | `prod:send` | screenProductionSend | ✅ Работает |
| Загрузка | `prod:load` | screenProductionLoad | ⚠️ Заглушка (→ screenDashboard) |

---

### 🏗️ Прораб (prefix: `f`, роли: foreman1/2/3)
| Экран | Callback | Функция | Статус |
|-------|----------|---------|--------|
| Главное меню | `f:menu` | screenForemanMenu | ✅ Работает |
| Входящие | `f:inbox` | screenInbox | 🔴 BUG-005: ищет "foreman" вместо foreman1/2/3 |
| Отправить | `f:send` | screenForemanSend | ✅ Работает |
| Подать отчёт | `f:report` | screenForemanReportFacade | ✅ Полный FSM |
| → Выбор фасада | `f:rf:<id>` | screenForemanReportFloor | ✅ |
| → Выбор этажа | `f:rfl:<id>` | screenForemanReportInput | ✅ |
| → Ввод факта | `f:rv:<n>` | screenForemanReportConfirm | ✅ |
| → Сохранение | `f:rs:<n>` | saveForemanReport | ✅ |
| Фотоотчёт | `f:photo` | screenForemanPhoto | ✅ Полный FSM |
| → Тип фото | `f:pt:<type>` | startPhotoFSM | ✅ |
| → Фасад | `f:pf:<id>` | screenPhotoFloor | ✅ |
| → Этаж | `f:pfl:<id>` | screenPhotoUpload | ✅ |
| → Загрузка фото | (file upload) | handlePhotoFile | ✅ до 5 фото |
| → Комментарий | `f:pc:done/skip` | screenPhotoComment | ✅ |
| → Подтверждение | `f:pc:confirm` | handlePhotoConfirm | ✅ |
| Прогресс | `f:progress` | screenForemanProgress | ✅ Работает |

**Самая проработанная роль.** Два полных FSM: отчёт + фотоотчёт.

---

### 📁 ПТО (prefix: `pto`)
| Экран | Callback | Функция | Статус |
|-------|----------|---------|--------|
| Главное меню | `pto:menu` | screenPTOMenu | ✅ Работает |
| Входящие | `pto:inbox` | screenInbox | ✅ Работает |
| Отправить АОСР | `pto:send` | screenPTOSend | ✅ Работает |
| Реестр документов | `pto:registry` | screenPTORegistry | ⚠️ Только счётчик + ссылка на приложение |

---

### 🔍 Технадзор (prefix: `insp`)
| Экран | Callback | Функция | Статус |
|-------|----------|---------|--------|
| Главное меню | `insp:menu` | screenInspectorMenu | ✅ Работает |
| Входящие | `insp:inbox` | screenInbox | ✅ Работает |
| Предписание | `insp:send` | screenInspectorSend | ✅ Работает |
| Приёмка этапа | `insp:accept` | screenInspectorAccept | ⚠️ Заглушка (→ screenFacades) |
| История проверок | `insp:history` | screenInspectorHistory | ✅ Работает |

---

## Shared экраны (используются несколькими ролями)
| Экран | Функция | Кто использует |
|-------|---------|----------------|
| Дашборд | screenDashboard | Director, PM, Production, Generic |
| Алерты | screenAlerts | Director, PM, Foreman, Generic |
| Снабжение | screenSupply | Director, PM, Supply |
| Фасады | screenFacades | Director, Foreman, Inspector |
| Детали фасада | screenFacadeDetail | Director, Foreman |
| Согласования | screenApprovals | Director, PM |
| Задачи | screenTasks | PM, Generic |
| Журналы | screenDailyLogs | Director, PM, Foreman, Generic |
| Настройки | screenSettings | Director, PM, Foreman, Generic |
| Inbox | screenInbox | PM, OPR, KM, KMD, Supply, Prod, Foreman, PTO, Inspector |
| Прогресс | screenProgress | OPR, KM, KMD |

## FSM-потоки
| FSM | Состояния | Роли |
|-----|-----------|------|
| Document | DOC_UPLOAD → DOC_COMMENT → DOC_CONFIRM | Все (через DOC_FSM_MAP) |
| Photo | PHOTO_FACADE → PHOTO_FLOOR → PHOTO_UPLOAD → PHOTO_COMMENT → PHOTO_CONFIRM | Foreman |
| Report | REPORT_FACADE → REPORT_FLOOR → REPORT_INPUT → confirm → save | Foreman |
| Alert | ALERT_PRIORITY → ALERT_TITLE → save | Director, PM |
| Daily Log | LOG_ZONE → LOG_WORKS → LOG_WORKERS → save | Foreman |

## Документооборот (DOC_FSM_MAP) — 30 типов документов
Полностью маршрутизирован: каждый тип → label + recipients[].
Все проходят через единый FSM: выбор → загрузка файла → комментарий → подтверждение → отправка.

## 🔴 Найденные проблемы

### Критические
1. **BUG-005 подтверждён:** `screenForemanMenu` → `getInboxCount(project.id, "foreman")` — роли `foreman` нет, есть `foreman1/2/3`
2. **Shared `_shared/botFSM.ts` и `botScreens.ts` — мёртвый код.** Бот их не импортирует. Используют другую схему сессий (`telegram_id` vs `chat_id`), другие состояния. Полный дубль.

### Заглушки (экраны-пустышки)
3. `screenFinance` — "доступно в приложении"
4. `screenSupplyStatus` / `screenSupplyDeficit` — оба → screenSupply (один и тот же экран)
5. `screenProductionLoad` → screenDashboard (не загрузка производства)
6. `screenInspectorAccept` → screenFacades (не приёмка этапа)
7. `screenPTORegistry` — только счётчик + ссылка

### Логические ошибки
8. `pm:s:supply` → screenSupply (показывает дефицит, а не подкатегории отправки для снабжения)
9. `pm:s:prod` → screenDashboard (показывает дашборд, а не подкатегории отправки для производства)
10. Нет `pm:logs` в callback dispatcher (кнопка не привязана)

### Архитектурные
11. `_shared/botScreens.ts` создаёт свой Supabase client — лишний cold start
12. `_shared/botFSM.ts` использует `telegram_id` вместо `chat_id` — несовместимо с ботом
13. Бот не использует `_shared/validateTelegram.ts` (BUG-011)
