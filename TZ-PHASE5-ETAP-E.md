# ТЗ Этап E — AI-Диспетчер (Daily Watchdog)

> Контекст: STSphera управляет стройкой. Просрочки, незакрытые алерты, несданные отчёты — всё это теряется. AI-Диспетчер каждое утро анализирует все объекты и рассылает персонализированные сводки по ролям через Telegram-бота.

---

## Цель

Автоматический ежедневный агент, который:
1. Анализирует все активные проекты: прогресс vs план, просрочки, дефицит, алерты
2. Генерирует персонализированные сводки по ролям
3. Рассылает через TG-бота каждое утро (08:00 MSK)
4. Эскалирует нерешённые проблемы по цепочке

---

## 1. Новая Edge Function: `daily-watchdog`

### Триггер
Supabase cron (pg_cron) или внешний cron → вызов Edge Function каждый день в 08:00 MSK (05:00 UTC).

### Алгоритм

**Шаг 1: Сбор данных**
Для каждого активного проекта (status = 'active'):
```sql
-- Просроченные задачи
SELECT * FROM ecosystem_tasks
WHERE project_id = $1
  AND status != 'done'
  AND due_date < CURRENT_DATE;

-- Незакрытые алерты
SELECT * FROM alerts
WHERE project_id = $1
  AND status = 'open'
ORDER BY severity DESC, created_at;

-- Несданные дневные отчёты (вчера)
SELECT p.full_name, p.role
FROM profiles p
WHERE p.project_id = $1
  AND p.role IN ('foreman1','foreman2','foreman3')
  AND NOT EXISTS (
    SELECT 1 FROM daily_logs dl
    WHERE dl.user_id = p.user_id
      AND dl.project_id = $1
      AND dl.date = CURRENT_DATE - 1
  );

-- План-факт отклонения
SELECT wt.name, pf.plan_qty, pf.fact_qty,
  ROUND((pf.fact_qty::numeric / NULLIF(pf.plan_qty, 0)) * 100) as progress_pct
FROM plan_fact pf
JOIN work_types wt ON wt.id = pf.work_type_id
WHERE pf.project_id = $1
  AND pf.fact_qty < pf.plan_qty * 0.8; -- отставание > 20%

-- Дефицит материалов
SELECT name, required_qty, available_qty
FROM materials
WHERE project_id = $1
  AND available_qty < required_qty * 0.3; -- осталось < 30%
```

**Шаг 2: Анализ (LLM)**
Собранные данные → в промпт Claude/GPT:
```
Ты — AI-диспетчер строительной компании.
Проанализируй данные по объекту "{project.name}" и составь краткую сводку.

Данные:
- Просроченные задачи: {tasks}
- Открытые алерты: {alerts}
- Несданные отчёты: {missing_reports}
- Отставание по работам: {plan_fact_issues}
- Дефицит материалов: {material_deficit}

Формат ответа:
1. Общий статус (🟢/🟡/🔴) + одно предложение
2. Критичное (если есть) — максимум 3 пункта
3. Требует внимания — максимум 5 пунктов
4. Рекомендации — 1-2 конкретных действия

Будь кратким. Это для чтения на телефоне утром.
```

**Шаг 3: Персонализация по ролям**

Каждая роль получает СВОЮ сводку:

| Роль | Что получает |
|---|---|
| director | Портфель: статус всех объектов, критичные алерты, KPI |
| project_manager | Детали по своим объектам: просрочки, отставание, дефицит |
| foreman1/2/3 | Свои задачи на сегодня, незакрытые алерты своих зон |
| supply | Дефицит материалов, ожидаемые поставки |
| pto | Несданные отчёты, документы на проверке |
| inspector | Приёмки на сегодня, незакрытые замечания |

**Шаг 4: Рассылка через TG-бота**
Для каждого получателя:
1. Найти telegram_id в profiles
2. Отправить сообщение через Bot API (sendMessage, parse_mode: HTML)
3. Логировать в bot_audit_log

### Формат сообщения (пример для директора)
```
🏗 Утренняя сводка — 15.03.2026

📊 Портфель: 5 объектов
🟢 ЖК Солнечный — в графике (67%)
🟡 БЦ Меридиан — отставание 12% (НВФ)
🔴 ТЦ Галактика — 3 критичных алерта

⚠️ Критичное:
• ТЦ Галактика: дефицит кронштейнов (осталось на 2 дня)
• БЦ Меридиан: прораб Петров не сдал отчёт 3 дня
• ЖК Солнечный: просрочена приёмка фасада (блок А)

📋 Рекомендации:
• Связаться с поставщиком кронштейнов (ТЦ Галактика)
• Эскалировать отчётность Петрова

[Открыть в приложении]
```

Кнопка "Открыть в приложении" → deep link на Mini App.

---

## 2. Эскалация

### Логика
Таблица `escalation_rules` (или hardcoded на старте):

| Просрочка | Действие |
|---|---|
| 1 день | Напоминание исполнителю |
| 3 дня | Уведомление РП + пометка в сводке |
| 7 дней | Уведомление директору + 🔴 в сводке |
| 14 дней | Повторное уведомление директору + предложение снять задачу |

### Реализация
```sql
-- Трекинг эскалаций
CREATE TABLE IF NOT EXISTS escalations (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  project_id uuid REFERENCES projects(id) NOT NULL,
  entity_type text NOT NULL, -- 'task', 'alert', 'report'
  entity_id uuid NOT NULL,
  level int NOT NULL DEFAULT 1, -- 1=исполнитель, 2=РП, 3=директор
  escalated_to uuid REFERENCES auth.users(id),
  escalated_at timestamptz DEFAULT now(),
  resolved_at timestamptz,
  notes text
);

ALTER TABLE escalations ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Project members can view escalations"
  ON escalations FOR SELECT
  USING (
    project_id IN (
      SELECT project_id FROM profiles WHERE user_id = auth.uid()
    )
  );

CREATE INDEX IF NOT EXISTS idx_escalations_project
  ON escalations(project_id, resolved_at NULLS FIRST);

CREATE INDEX IF NOT EXISTS idx_escalations_entity
  ON escalations(entity_type, entity_id);
```

---

## 3. Фронтенд: DigestPage (история сводок)

### Точка входа
Меню объекта → "📊 Сводки" (новый пункт в секции "Инструменты")

### Экран: список сводок
- Вертикальный список по датам (последние 30 дней)
- Каждая карточка:
  - Дата: "15 марта 2026"
  - Статус: 🟢/🟡/🔴 + краткое описание
  - Метрики: просрочки / алерты / отставание
  - Тап → детальная сводка

### Экран: детальная сводка
- Полный текст AI-анализа (markdown render)
- Секции: Критичное → Требует внимания → Рекомендации
- Список эскалаций за этот день
- Кнопки действий: "Написать в TG" (deep link на чат проекта)

### Визуал MONOLITH
- Карточка сводки: `bg-bg1`, `rounded-2xl`, `p-4`
- Статус dot: `w-3 h-3`, `rounded-full`, цвет по статусу
- Дата: `text-[15px]`, `font-semibold`, `text-t1`
- Метрики: `text-[12px]`, `text-t3`, inline badges
- Детальный текст: `text-[14px]`, `text-t2`, `leading-relaxed`

---

## 4. Миграция БД

```sql
-- Хранение сводок
CREATE TABLE IF NOT EXISTS daily_digests (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  project_id uuid REFERENCES projects(id) NOT NULL,
  date date NOT NULL DEFAULT CURRENT_DATE,
  status text NOT NULL DEFAULT 'green', -- green, yellow, red
  summary text NOT NULL, -- AI-сгенерированный текст
  data jsonb DEFAULT '{}'::jsonb, -- сырые данные анализа
  metrics jsonb DEFAULT '{}'::jsonb, -- {overdue_tasks, open_alerts, plan_deviation_pct, deficit_items}
  created_at timestamptz DEFAULT now(),
  UNIQUE(project_id, date)
);

ALTER TABLE daily_digests ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Project members can view digests"
  ON daily_digests FOR SELECT
  USING (
    project_id IN (
      SELECT project_id FROM profiles WHERE user_id = auth.uid()
    )
  );

CREATE INDEX IF NOT EXISTS idx_daily_digests_project_date
  ON daily_digests(project_id, date DESC);

-- Лог рассылок
CREATE TABLE IF NOT EXISTS digest_deliveries (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  digest_id uuid REFERENCES daily_digests(id) NOT NULL,
  user_id uuid REFERENCES auth.users(id) NOT NULL,
  telegram_chat_id bigint,
  delivered_at timestamptz,
  status text DEFAULT 'pending', -- pending, sent, failed
  error text
);

CREATE INDEX IF NOT EXISTS idx_digest_deliveries_digest
  ON digest_deliveries(digest_id);
```

---

## 5. Настройки уведомлений

Использовать существующую таблицу `notifications_config`:
- Добавить поле: `digest_enabled boolean DEFAULT true`
- Добавить поле: `digest_time time DEFAULT '08:00'` (персональное время)
- DND часы уже есть — диспетчер их учитывает

---

## 6. Два режима работы

**Режим A (MVP):** чистая логика без LLM
- SQL-запросы → форматирование по шаблону → рассылка
- Быстро, дёшево, предсказуемо
- Достаточно для старта

**Режим B (полный):** с LLM-анализом
- SQL-запросы → LLM анализ → персонализированные рекомендации
- Умнее, но дороже (~$0.05-0.10 за сводку)
- Включать когда нормативная база наполнена (Этап B+C)

Рекомендация: начать с режима A, добавить B позже переключателем в настройках.

---

## 7. Порядок работ

1. Миграция БД (daily_digests, escalations, digest_deliveries, ALTER notifications_config)
2. Edge Function `daily-watchdog` — режим A (SQL + шаблоны)
3. Cron: pg_cron или Supabase scheduled function (05:00 UTC = 08:00 MSK)
4. Фронтенд: DigestPage (список + детали)
5. Тест: запустить вручную → проверить сводку + TG-рассылку
6. Позже: добавить режим B (LLM), эскалации, персональное время
