// Placeholder — will be replaced with full implementation
const GamificationPanel = ({ userId, projectId, userRole }: { userId: string; projectId: string; userRole: string }) => (
  <div className="p-4 text-muted-foreground text-sm">GamificationPanel — ожидает код</div>
);
export default GamificationPanel;
