// Placeholder — will be replaced with full implementation
const ReportPDF = ({ projectId, projectName }: { projectId: string; projectName: string }) => (
  <div className="p-4 text-muted-foreground text-sm">ReportPDF — ожидает код</div>
);
export default ReportPDF;
