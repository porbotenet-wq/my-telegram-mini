// Placeholder — will be replaced with full implementation
const DirectorDashboard = ({ onOpenProject }: { onOpenProject: (id: string) => void }) => (
  <div className="p-4 text-muted-foreground text-sm">DirectorDashboard — ожидает код</div>
);
export default DirectorDashboard;
